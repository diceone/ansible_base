---
- hosts: all
  tasks:
  - name: ensure basic packages installed
    yum:
      name: "{{ packages }}"
    vars:
      packages:
      - jq
      - net-tools
      - htop
      - vim
      - epel-release
